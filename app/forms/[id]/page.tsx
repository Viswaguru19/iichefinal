'use client';

/**
 * Public form URL for QR codes and share links (no /dashboard → no login redirect).
 * Same implementation as dashboard form fill page.
 */
export { default } from '@/app/dashboard/forms/[id]/page';
